import React, { Component } from 'react';
import { Button, Card, CardText } from 'react-mdl';

let four_hours = ["M,W", "M,Th", "T,F", "T,Th", "W,S"]
let three_hour = ["M,W", "M,Th", "M,W,Th", "T,W,F", "T,F", "T,Th", "W", "W,S"]

class Hours extends Component {
    return (
        <div>
          <Button> </Button>
          <Button> </Button>
        </div>
    );
}

export default Hours;
